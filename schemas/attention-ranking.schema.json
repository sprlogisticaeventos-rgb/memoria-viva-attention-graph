{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:memoria-viva:attention-graph:schema:attention-ranking:1.0.0",
  "title": "AttentionRanking",
  "description": "An immutable deterministic computed production ranking for one Snapshot and exact policy identity.",
  "x-schema-version": "1.0.0",
  "type": "object",
  "properties": {
    "schema_version": {"type": "string", "const": "1.0.0"},
    "contract_kind": {"type": "string", "const": "COMPUTED_PRODUCTION_RANKING"},
    "attention_ranking_id": {"$ref": "urn:memoria-viva:attention-graph:schema:common:1.0.0#/$defs/repository_safe_reference"},
    "ranking_version": {"type": "integer", "minimum": 1},
    "ranking_context": {"type": "string", "enum": ["before", "after"]},
    "snapshot_id": {"$ref": "urn:memoria-viva:attention-graph:schema:common:1.0.0#/$defs/repository_safe_reference"},
    "policy_id": {"$ref": "urn:memoria-viva:attention-graph:schema:common:1.0.0#/$defs/repository_safe_reference"},
    "policy_version": {"type": "string"},
    "policy_digest": {"type": "string", "minLength": 1},
    "deterministic_input_digest": {"type": "string", "minLength": 1},
    "items": {
      "type": "array",
      "minItems": 1,
      "items": {"$ref": "urn:memoria-viva:attention-graph:schema:attention-item:1.0.0"}
    },
    "evidence_refs": {"$ref": "urn:memoria-viva:attention-graph:schema:common:1.0.0#/$defs/nonempty_evidence_refs"},
    "generated_at": {"$ref": "urn:memoria-viva:attention-graph:schema:common:1.0.0#/$defs/timestamp"},
    "tie_break_method": {"type": "string"}
  },
  "required": [
    "schema_version",
    "contract_kind",
    "attention_ranking_id",
    "ranking_version",
    "ranking_context",
    "snapshot_id",
    "policy_id",
    "policy_version",
    "policy_digest",
    "deterministic_input_digest",
    "items",
    "evidence_refs",
    "generated_at",
    "tie_break_method"
  ],
  "additionalProperties": false
}
