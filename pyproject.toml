[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "memoria-viva-attention-graph"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["jsonschema>=4,<5"]

[tool.setuptools.packages.find]
where = ["src"]
